# CEMS Chinese PWA 9.5.1

v9.5.0 에서 **실제로 죽어 있던 기능들**을 고친 릴리스입니다. 화면 구성과 겉모습은 그대로입니다.

모든 항목은 실제 Chromium 으로 재현한 뒤 고치고, 같은 검사를 수정 전 트리에서도 돌려
**수정 전에는 FAIL 이 나는 것**을 확인했습니다. 검사는 `harness/` 에 들어 있습니다.

---

## 앱이 멈추던 문제

**확인 대화상자를 여는 순간 앱 전체가 정지했습니다.** "학습 종료 → 종료하기" 뿐 아니라
삭제·초기화·복원 대화상자도 전부 같았습니다.

`v944/cems-v9.4.4.js` 의 관찰자가 `#confirm-modal` 의 `class` 를 보면서, 콜백에서
`enhanceNativeModals()` 를 불러 그 `class` 를 다시 썼습니다. Chromium 은 **이미 있는
토큰을 `classList.add` 해도 MutationRecord 를 쌓습니다**(하네스로 실측). 마이크로태스크
체크포인트는 큐가 빌 때까지 태스크 루프로 돌아가지 않으므로 메인 스레드가 그대로 멈췄습니다.

- 이미 있으면 쓰지 않도록 바꿨고, 콜백에 재진입 가드와 `takeRecords()` 를 넣었습니다.
- `harness/modals.mjs` 를 새로 넣었습니다. 확인 대화상자 5종과 학습 모드 12종의
  "학습 종료" 를 실제 DOM 클릭으로 돌리고, **모든 `page.evaluate` 에 타임아웃을 걸어**
  메인 스레드가 멈추면 FAIL 로 잡습니다. 기존 `probe`/`sweep` 은 학습 세션 화면에
  들어가지 않아 이 결함을 한 건도 잡지 못했습니다.

## 화면 진입 속도

**Lean 학습 화면 진입 39,761ms → 5,338ms.**

원인은 DB 조회가 아니었습니다. 문서 전체를 훑는 `phase7UpdateA11yState()` 가
DOM 노드 하나가 늘어날 때마다 불렸습니다. 진입 1회 실측:

| | 수정 전 | 수정 후 |
|---|---|---|
| `phase7UpdateA11yState` 실행 | 24,638회 | 24,630회 (사실상 무비용) |
| `document.querySelectorAll` | 98,792회 | 263회 |
| `setAttribute` | 12,606,531회 | 1,556회 |
| `inert` 설정 | 542,036회 | 1회 |

프레임당 1회로 합치고(배경 탭용 타이머 백업 포함), 값이 실제로 달라질 때만 쓰도록 했습니다.
다른 화면은 그대로입니다(통계 103ms · 데이터 144ms · 홈 126ms · 학습 184ms).

저장 경로도 하나 줄었습니다. IndexedDB 에 쓰는 모든 문자열 필드마다 HTML 파서를 돌리고
있었는데(시드 3,000행 = 필드 49,469개에 550ms, 그중 마크업이 있는 필드는 0개),
`<` 도 `&` 도 없는 문자열은 파서를 건너뜁니다 — 550ms → 41ms. 결과가 같다는 것은
실제 시드 전 필드 49,196개 대조로 확인했습니다(`harness/sanitizer.mjs`).

## 조용히 죽어 있던 기능

- **데이터 화면의 "추가" 버튼** — 이 빌드에 없는 `#add-type-phrasal` 을 참조해 예외가 나서
  모달이 열리지 않았습니다. 수동 단어·표현 추가가 통째로 불가능했습니다.
- **학습 모드 선택기의 "확인" 버튼** — 감추려던 인라인 `display:none` 이 v944 CSS 의
  `!important` 에 져서 계속 보였고, **직전 대화상자의 onclick 이 그대로 남아 있었습니다**.
  선택기에서 확인을 누르면 직전에 취소했던 "학습 종료" 가 실행됐습니다.
- **AI 문장 채점** — 네 군데가 막고 있었습니다.
  1. Worker 주소와 토큰이 저장되지 않았습니다(설정 저장 목록에 두 입력칸이 빠져 있었습니다).
  2. 채점에 성공하면 "다음 문장" 버튼이 disabled 로 남아 다음 문장으로 넘어갈 수 없었습니다.
  3. Worker 실패 1회마다 미처리 예외가 하나씩 떠서 앱 크래시로 기록됐습니다.
  4. "연결 확인" 이 `/health` 200 만 보고 "연결 성공" 이라고 했습니다. 이제 채점기 버전
     불일치·API 키 없음·Worker 경고를 검사합니다. 오류도 코드(`upstream_error`) 대신
     한국어로 보여줍니다.
- **학습 경과 시간** — 탭을 한 번만 벗어나면 표시가 그대로 멈췄습니다(숨길 때 타이머를
  지우고 돌아올 때 되살리지 않았습니다).

## 데이터 정확성

- **다른 탭이 DB 를 업그레이드하면 이 탭이 데이터 0건으로 보였습니다.** `versionchange`
  처리기가 `db.close()` 만 하고 핸들을 남겨서, 이후 모든 조회가 `InvalidStateError` 를
  삼키고 빈 배열을 돌려줬습니다(9,447 → 0). 이제 다음 조회가 새 버전으로 다시 엽니다.
- **"최근 7일" 데이터 필터가 다른 컬렉션까지 걸렀습니다.** 렌즈 콜백이 `kind` 를 보지 않아,
  필터가 걸린 약 2초 동안 앱 어디서 부르든 `getAllExpr` 이 걸러진 결과를 돌려줬습니다
  (표현 1678 → 1278). 시드를 방금 넣은 프로필에서는 보이지 않습니다 — 모든 행의 추가일이
  오늘이라 필터가 아무것도 걸러내지 않기 때문입니다.
- **빈 답이 만점으로 채점됐습니다.** 병음이 없는 카드에서 정규화 결과가 양쪽 다 빈 문자열이
  되어 "일치" 가 됐습니다. 내장 시드에는 병음 없는 행이 없지만 **가져온 데이터에는 있을 수
  있습니다**. 타이핑·받아쓰기·중국어 집중 모드 모두 막았습니다.
- **화면 이동으로 끝낸 표현 받아쓰기가 어휘로 기록됐습니다.** 그 분기만
  `currentListeningType` 을 쓰고 있었습니다.
- 답안 제출 잠금을 풀지 않는 조기 반환 11곳을 닫았습니다. (UI 로 재현하지는 못했지만
  계약이 깨져 있었습니다.)

## 표시되는 버전

**9.5.0 빌드가 사용자에게 "v9.4.4" 로 보이고 있었습니다** — 탭 제목, 스플래시, 홈 브랜드
문구, 설정의 버전 정보 카드, 앱 빌드 항목 전부. 같은 DOM 에 버전을 쓰는 모듈이 여섯이었고
각자 자기 상수를 썼습니다. `data-cems-version` 은 두 값 사이를 오갔습니다(7개 화면 이동에
63회 쓰기).

이제 출처는 `index.html` 의 `<html data-cems-version>` 하나이고, 나머지는 읽기만 합니다.
`harness/version.mjs` 가 실제 브라우저에서 이를 확인합니다.

## 서비스워커

- `cache:'no-store'` 로 요청한 것까지 stale-while-revalidate 로 캐시하고 있었습니다.
  최신값이 목적인 요청에 오래된 응답이 돌아갔습니다. 내비게이션은 오프라인 셸 폴백이
  필요하므로 그대로 둡니다.
- 캐시 버전을 `cems-zh-v9-9.5.1` 로 올렸습니다. **버전을 올리지 않으면 이 수정들이 기존
  캐시에 가려 사용자에게 가지 않습니다.**

## 검증

`harness/` 에 네 개를 새로 넣고 기존 것에 다섯 항목을 더했습니다.
전부 `npm run all` 로 한 번에 돌아갑니다.

| 스크립트 | 무엇을 지키는가 |
|---|---|
| `modals.mjs` (신규) | 확인 대화상자 · 학습 종료 12모드 (실제 DOM 클릭, 정지 감지) |
| `version.mjs` (신규) | 화면 버전 문구 ↔ `VERSION` 파일 |
| `ai-grader.mjs` (신규) | AI 채점 전 경로 — 계약을 지키는 모의 Worker, Gemini 키 불필요 |
| `sanitizer.mjs` (신규) | 저장 정규화 빠른 경로의 동치성 (49,196개 입력) |
| `probe.mjs` | + 빈 답 채점 · 탭 복귀 타이머 · 렌즈 격리 · 강제 종료 종류 · DB 복구 |
| `asset-check.mjs` | + `CEMS_IMPORT_CORE` 두 사본 바이트 대조 |

현재 상태: 문법 29/0 · 자산 대조 통과 · probe 전 항목 통과 · modals 21/21 ·
ai-grader 10/10 · sanitizer 차이 0 · sweep CRIT 0 / HIGH 0 ·
미처리 예외 0 · 콘솔 오류 0.

하네스는 `CEMS_CHROMIUM_PATH` 환경변수로 미리 설치된 Chromium 을 쓸 수 있습니다.

## 이번 범위에 넣지 않은 것

- **시드 콘텐츠 품질** — 어휘 9,447행 중 약 절반의 한국어 뜻이 번역이 아니라
  `병음 · 품사 · 주제` 조립 문자열이라 병음 정답이 문제에 노출됩니다. 코드가 아니라
  데이터 작업이고, 빌드 입력(`content/cems_zh_full_seed_v932.json`)이 저장소에 없어
  재생성이 불가능합니다.
- **연어 모드** — 시드에 연어가 55개(0.58%)뿐입니다. 콘텐츠 문제입니다.
- **구동사(phrasal) 코드** — 이 빌드는 해당 화면 마크업이 없어 `pv-*` / `pvp-*` / `pvq-*`
  를 참조하는 코드 약 2,000줄이 도달 불가능한 죽은 코드로 남아 있습니다
  (UI 가 노출하는 종류는 단어·표현·문법뿐). 동작에 영향은 없지만 정리 대상입니다.
