# CEMS Chinese PWA 9.5.0

v9.4.4-final2 까지 누적된 "전역 함수 덮어쓰기" 확장 방식을 걷어내고, 감사에서 확인된 결함을 수정한 구조 정리 릴리스입니다. 화면 구성과 겉모습은 그대로입니다.

## 구조 — 몽키패칭 제거

- 전역 함수의 소유자를 `index.html` 하나로 고정하고, 확장 모듈은 **훅 레지스트리**(`CEMSHooks`)에 키로 등록만 합니다. 같은 키로 다시 등록하면 교체되므로 재설치 타이머가 있어도 중첩되지 않습니다.
- 채널: `beforePageShow` · `afterPageShow` · `afterTypeSwitch` · `studySelection`(변환) · `quizItems`(변환) · `afterShowCard` · `afterShowExprCard` · `afterTypingQuestion` · `afterBookmarkToggle`
- 조회 결과만 걸러야 하는 곳은 전역 함수를 바꿔치기하지 않고 **데이터 렌즈**(`CEMS_LENS`)를 씁니다. 고유 id 로 등록·해제하므로 동시 호출이 서로를 덮어쓰지 않습니다.
- 재설치 타이머 3종(`[700,1500,3000]` · `[900,2200]` · `[450,1600,3600]`)과 `500ms×8` 루프를 제거했습니다.
- `eval(name + ' = wrapped')` 3곳을 제거했습니다.

실측: `showPage` 1회 → `history.replaceState` **5회 → 1회**, 탭 전환 1회 → `updateWordTable` **5회 → 1회**.

## 치명 결함 수정

- **AI 문장 채점이 다시 동작합니다.** 클라이언트와 Worker 의 `GRADER_VERSION` 불일치로 모든 요청이 409 로 거부되고 있었습니다. verdict 열거도 맞추고, 실패한 요청이 일일 한도를 소진하던 문제도 고쳤습니다.
- **"업데이트" 버튼이 실제로 새로고침합니다.** 적용 경로가 `phase7UpdateRequested` 를 세우지 않아, 새 서비스워커가 활성화돼도 구 코드가 새 캐시 위에서 계속 돌았습니다.
- **시드 무결성 복구가 실제 DB 를 찾습니다.** 후보 목록에 실제 이름(`ChineseVocab_v1`)이 없어 항상 아무 일도 하지 않았습니다. 병합 방향(기존 값이 시드를 덮던 문제)과 저장 실패 집계도 함께 고쳤습니다.
- **표현·문법 목록 오염을 없앴습니다.** 탭 전환 시 전역 조회 함수 복원이 어긋나 앱 전체가 걸러진 목록만 보게 되던 문제입니다.
- **가져온 데이터가 속성 위치로 그대로 들어가던 지점에 이스케이프를 적용**했습니다.

## 학습 동작

- 퀴즈 카드 보충이 모드별 필수 데이터를 검사합니다. 연어 모드에서 연어가 없는 카드가 출제되던 문제가 사라집니다.
- 랜덤 회전이 카드덱 프리셋도 인식합니다.
- 문제가 준비되지 않은 상태에서 "확인"/"힌트" 를 눌러도 오류가 나지 않습니다.
- 숫자키 1–4 단축키가 입력란을 삼키지 않습니다.
- 루틴 타이머 일시정지가 실제로 멈춥니다.
- Lean 실행이 사용자의 전역 학습 개수 설정을 덮어쓰지 않습니다.
- 외부 JSON 소스 하나를 지워도 내장 학습의 랜덤 회전 이력이 남습니다.

## 데이터 가져오기

- Worker 파서와 메인스레드 대체 파서가 **같은 정규화·병합 키**를 씁니다. 같은 파일을 두 경로로 넣어도 항목이 둘로 갈라지지 않습니다.
- 저장 실패 시 Worker 가 영구 정지하던 교착을 없앴습니다.
- 예문 병음을 단어 병음으로 저장하던 오류를 고쳤습니다.

## 저장·오프라인

- 저장 공간이 한 번 찼다고 이후 모든 변경이 메모리에만 남던 문제를 고쳤습니다(복구 시 되돌려 씁니다).
- 지연 콘텐츠(14.6MB 시드) 내려받기가 중단돼도 다음 요청에서 이어받습니다. 첫 실행 중복 내려받기도 합칩니다.
- 서비스워커가 오류 응답에서 캐시된 셸로 폴백합니다. 구버전 캐시 정리 범위도 넓혔습니다.
- 복구 파일이 오프라인 캐시 목록에 포함됩니다.

## Worker 보안

- 요청 본문·필드 길이 상한, 레이트리밋 바인딩, 업스트림 인증 오류 구분, 타임아웃 오분류로 인한 중복 호출 제거.
- `workers_dev: false`, 로그 활성화, CPU 상한을 설정 파일에 추가했습니다.

## 디자인

겉모습은 그대로 두고 구조만 정리했습니다.

- `!important` 1,255개 → 1,050개. 제거한 205개는 **제거해도 실제 렌더가 달라지지 않음을 브라우저 computed style 대조로 확인**한 것들입니다.
- 학습 액션 아이콘이 "규칙 순서가 바뀌면 전부 사라지는" 상충 구조였습니다. 상충 자체를 없앴습니다.
- 북마크 켜짐/꺼짐이 시각적으로 구분되지 않던 문제를 고쳤습니다.
- 키보드 포커스 표시와 "움직임 최소화" 설정 대응을 추가했습니다.

## 검증

`harness/` 에 실제 Chromium 회귀 테스트를 넣었습니다. 이번 릴리스는 다음을 실행해 확인했습니다.

- 문법 검사 29개 통과 · 서비스워커 선언 자산 49개 대조 통과
- 화면 10개 · 학습 모드 8종 전수 · 미처리 예외 0건 · 콘솔 오류 0건
- CSS 렌더 대조: 원본 CSS 대비 차이 0건

## 알려진 제한

- **Lean 학습 화면 진입이 약 60초 걸립니다.** v9.4.4-final2 에서도 동일하며(측정 57.5초), 이번 릴리스에서 고치지 않았습니다.
- 연어 모드는 시드에 연어 데이터가 55개뿐이라 사실상 콘텐츠가 없습니다. 데이터 작업이 필요합니다.
- 시드 콘텐츠 품질(한국어 뜻 절반이 조립 문자열)은 이번 범위에 넣지 않았습니다.
